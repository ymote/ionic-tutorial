**1.** Finish the `searchMovies` method in **MovieService** to search movies on titles, directors and actors.

**2.** In **MovieHomeCtrl**, call the method from **1.** in `$scope.$watch` block to update movies. 